#include <iostream>
using namespace std;
#include "District.h"
#include "vehicleFlash.h"


void main() {
	setlocale(LC_ALL, "Rus");
	//vehicleFlash one(0, 0, 0);
	//one.setCondition(0, 0, 1);
	District centr;
	centr.day();
}